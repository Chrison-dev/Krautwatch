<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Domain\Krautwatch.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="FluentValidation" />
    <PackageReference Include="FluentValidation.DependencyInjectionExtensions" />
    <!-- BackgroundService + ILogger for the crawl scheduler (Application stays transport-agnostic) -->
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" />
    <!-- Source-generated object mapping (entity → DTO) -->
    <PackageReference Include="Riok.Mapperly" />
  </ItemGroup>

  <ItemGroup>
    <!-- So masking can be unit-tested directly without widening the public API for it. -->
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
      <_Parameter1>Krautwatch.Application.Tests</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>

</Project>
