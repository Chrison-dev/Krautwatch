<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Domain\Krautwatch.Domain.csproj" />
  </ItemGroup>

  <ItemGroup Label="EF Core — provider-agnostic (DR-009): Postgres default, SqlServer swappable by config">
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Configuration.Binder" />
    <!-- The ZDF adapter owns the "our key was rejected" condition, so it also owns the health check
         that reports it (#13). Registration only — the hosts still map the endpoints. -->
    <PackageReference Include="Microsoft.Extensions.Diagnostics.HealthChecks" />
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Http" />
    <PackageReference Include="Microsoft.Extensions.Identity.Core" />
    <PackageReference Include="Microsoft.Extensions.Caching.Memory" />
  </ItemGroup>

  <ItemGroup Label="TheTVDB — the ITvdbCatalog adapter (Chrison-dev/TvdbApi)">
    <PackageReference Include="TvdbClient" />
    <PackageReference Include="TvdbClient.Abstractions" />
    <PackageReference Include="TvdbClient.Models" />
  </ItemGroup>

  <ItemGroup Label="Messaging — dispatch adapter for the IMessageDispatcher port (DR-009 §5)">
    <PackageReference Include="WolverineFx" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Krautwatch.Infrastructure.Tests" />
  </ItemGroup>

</Project>
